# Copilot Instructions

## Project Guidelines
- Use methods and functions to break down complex problems into smaller, manageable pieces.
- Use `var` for variables instead of the specific type.
- Use full names for variables (e.g. "videoEvent", not "ve") to improve readability.